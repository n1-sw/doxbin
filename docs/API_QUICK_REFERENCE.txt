╔════════════════════════════════════════════════════════════════════════════╗
║                     SCXBIN - API QUICK REFERENCE                           ║
╚════════════════════════════════════════════════════════════════════════════╝

┌─ AUTHENTICATION ─────────────────────────────────────────────────────────┐
│ POST   /api/register           → Create new account                       │
│ POST   /api/login              → Login & create session                   │
│ POST   /api/logout             → Logout & destroy session                 │
│ GET    /api/user               → Get current user info                    │
└─────────────────────────────────────────────────────────────────────────────┘

┌─ POSTS ──────────────────────────────────────────────────────────────────┐
│ GET    /api/posts              → Get all posts (sorted by date)           │
│ GET    /api/posts/:id          → Get single post (increments views)      │
│ POST   /api/posts              → Create post (admin only, supports media) │
│ DELETE /api/posts/:id          → Delete post (admin only)                │
└─────────────────────────────────────────────────────────────────────────────┘

┌─ COMMENTS ───────────────────────────────────────────────────────────────┐
│ GET    /api/posts/:id/comments → Get all comments for post               │
│ POST   /api/posts/:id/comments → Create comment (authenticated users)    │
└─────────────────────────────────────────────────────────────────────────────┘

┌─ REAL-TIME (WebSocket / Socket.IO) ──────────────────────────────────────┐
│ EVENT  load_messages           → Request chat history                     │
│ EVENT  send_message            → Send new chat message                    │
│ EVENT  new_message             → Receive new messages                     │
│ EVENT  user_connected          → Notification of new user                 │
└─────────────────────────────────────────────────────────────────────────────┘

┌─ UTILITIES ──────────────────────────────────────────────────────────────┐
│ GET    /api/posts/:id/views    → Get real-time view count               │
│ GET    /health                 → Server health check                      │
└─────────────────────────────────────────────────────────────────────────────┘

═════════════════════════════════════════════════════════════════════════════

EXTERNAL APIs (Optional Integrations):

📧 EMAIL SERVICE        - For verification & notifications
                        (SendGrid, Mailgun, etc.)

☁️  FILE STORAGE        - For image/video uploads  
                        (Cloudflare R2, AWS S3)

🔐 OAUTH                - Social login
                        (Google, GitHub, Microsoft)

📊 ANALYTICS            - User tracking
                        (Google Analytics, Mixpanel)

🔍 SEARCH               - Full-text search
                        (Elasticsearch, Algolia)

═════════════════════════════════════════════════════════════════════════════

DATABASE CONNECTIONS:

🗄️  PostgreSQL          - Current production (user data, posts, comments)
🟢 MongoDB             - Optional enhancement (user profiles, analytics)
⚪ Cloudflare D1       - For Workers deployment (SQLite-based)

═════════════════════════════════════════════════════════════════════════════

AUTHENTICATION REQUIREMENTS:

🔓 PUBLIC ENDPOINTS
  - GET /api/posts
  - GET /api/posts/:id
  - GET /api/posts/:id/comments
  - GET /api/posts/:id/views
  - POST /api/register
  - POST /api/login

🔐 AUTHENTICATED (Logged-in users)
  - POST /api/posts/:id/comments
  - POST /api/logout
  - GET /api/user

👤 ADMIN ONLY
  - POST /api/posts
  - DELETE /api/posts/:id

═════════════════════════════════════════════════════════════════════════════
